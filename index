#!/usr/bin/env bash

# stop executing script on error
# set -e


source ./functions/tasks
source ./functions/cntlm
source ./functions/messages

  # # Prevent sudo timeout
  # sudo -v # ask for sudo password up-front
  # while true; do
  #   # Update user's timestamp without running a command
  #   sudo -nv; sleep 1m
  #   # Exit when the parent process is not running any more. In fact this loop
  #   # would be killed anyway after being an orphan(when the parent process
  #   # exits). But this ensures that and probably exit sooner.
  #   kill -0 $$ 2>/dev/null || exit
  # done &

# case $1 in
#   '--proxy')
#     task_cntlm_install
#     task_cntlm_config
#     ;;
#   '--base')
#     task_mirrors
#     task_install_base
#     task_language
#     task_fonts
#     task_autostart
#     task_webstorm
#     task_diodon
#     task_middleClick
#     task_npm
#     task_gsettings
#     task_settings
#     task_dconf
#     task_terminal
#     task_alternatives
#     task_android
#   	;;
#   '--advanced')
#     task_folders
#     task_install_advanced
#     task_git
#     task_dreymar
#     task_themeing
#     task_byobu
#   	;;
#   '--optional')
#   	task_install_optional
#   	;;
#   '--kernel')
#   	task_kernel
#   	;;
#   *)
#   	echo "No arguments given."
#     show_help
#   	;;
# esac
#
# source "$HOME/.bashrc"

show_success
show_success "******************************************************************************"
show_success "******************************************************************************"
show_success "UBUNTU POST INITIALIZATION SUCCESSFULL! "
show_success "Things you will have to do manually: "
show_success "============================================================================== "
show_success "- Install Thunderbird addons: Minimize to Tray, Lightning, add email accounts "
show_success "- Import compiz config settings from file 'data/ccsm.profile'"
show_success "- Configure guake preferences"
show_success "- Copy colors from guake bluloco to gnome-terminal "
show_success "- REBOOT!"
show_success

# TODO
# atom settings
# exit on error
# grob minimal version
